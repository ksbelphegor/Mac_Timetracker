#!/bin/bash
# =============================================================================
# Mac Time Tracker
# =============================================================================
# setup.command는 더 이상 필요하지 않습니다.
# start.command가 첫 실행 시 필요한 패키지를 자동으로 설치합니다.
#
# 그냥 start.command를 더블클릭하세요!
# =============================================================================

SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
open "$SCRIPT_DIR/start.command"
